#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
	vector<int> twoSum(vector<int> &numbers, int target) {
		
	}
};

int main(void) {
	int v1[10] = {1,5,9,2,5,7,4,3};
	Solution s;
	vector<int> case1(&v1[0], &v1[7]); 

	vector<int>::iterator it;
	for(it = case1.begin(); it != case.end(); it++)
		cout << *it << endl;
	return 0;
}
